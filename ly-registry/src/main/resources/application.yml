server:
  port: 10086
spring:
  application:
    name: ly-registry
eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:10086/eureka
    #不注册到eureka服务就不需要  拉取服务列表
    fetch-registry: false
    #是否注册到eureka服务
    register-with-eureka: false